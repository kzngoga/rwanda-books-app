import React from 'react';
import { View, Text } from 'react-native';

const BooksScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: '#ffffff',
        alignItems: 'center',
      }}
    >
      <Text style={{ color: '#343a40' }}>About Screen</Text>
    </View>
  );
};

export default BooksScreen;
